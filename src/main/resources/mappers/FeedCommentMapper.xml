<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.greengram3.feed.FeedCommentMapper">
    <insert id="insFeedComment" useGeneratedKeys="true" keyProperty="ifeedComment"> <!-- 여기서는 _가 필요없음 -->
        INSERT INTO t_feed_comment
        SET iuser = #{iuser}, ifeed = #{ifeed}, comments = #{comment}
    </insert>
    <select id="selFeedCommentAll">
        <!-- primary key, comment, created_at, primary key iuser, name, pic(profile), 페이징 처리 -->
        SELECT A.ifeed_comment AS ifeedComment, A.comments AS comment, A.created_at AS createdAt, B.iuser AS writerIuser, B.nm AS writerNm, B.pic
        AS writerPic
        FROM t_feed_comment A
        JOIN t_user B ON A.iuser = B.iuser
        WHERE A.ifeed = #{ifeed}
        ORDER BY A.ifeed
        LIMIT #{StartIdx}, #{rowCount}
    </select>
</mapper>